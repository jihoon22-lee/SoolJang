# v1.7.0 B05 — 다중 출처 탐색과 관심 저장

WP08 #126·WP09 #127, 진행 중. B04 `0e9ce05` 기반이며 B07/B08과 공유할 초기 관심
계약을 먼저 고정한다. 전체 탐색 구현/실연결/브라우저 수용은 아직 완료하지 않았다.

## 초기 관심 계약

`Interest`는 구매·제품 생성과 별도 UUID이며 name/identity/source_matches/note/archived_at/
nullable product_id를 갖는다. source_matches는 source UUID별 product_key(외부 제품),
external_url/external_key(원래 판매 항목)/external_name을 보존한다. ID가 없는 HTML 소스는
key null을 허용하고 실제 요청에서 host·DNS 정책을 다시 검증한다.

0016은 외부 offer/캐시에 interest_id를 추가하며 기존 product_id 또는 interest_id 하나만
가진다. 관심의 구매 전환 후에도 과거 offer/감시의 Interest.id 참조를 바꾸지 않는다.
새 관심 자료를 표현할 수 없는 구버전으로의 downgrade는 해당 신규 자료를 제거하므로
운영 자동 downgrade를 사용하지 않는다. 기존 제품/구매 참조는 보존한다.

최소 GET/POST/PATCH API는 사용자 소유권·공개 URL·수정 revision을 검증한다. 생성 요청의
request_id가 같으면 같은 관심 UUID를 돌려주며, 같은 키의 다른 입력은 충돌로 거절한다.
관심 등록만으로 구매·재고 합계가 늘지 않는다. B08은 이 계약 위에 구매 전환/목록 UX를 더한다.

검증: 관심 CRUD·반복 저장 및 0013→0016→0013→0016/drift 집중 5검사 통과.
Ruff·format·ty 검사 통과. 새 탐색 UI/서비스의 검증은 같은 문서에 이어 기록한다.

## Exa 환경 설정과 실제 확인

2026-09-07 사용자가 EXA_API_KEY 사용과 ~/.bashrc 등록을 승인했다. 다른 WSL 셸의 export는
기존 프로세스에 전달되지 않아 사용자가 직접 ~/.bashrc에 추가했다. 그 설정을 유지하고
임시 파일 로더 중복만 제거했다. ~/.bashrc 권한0600·Bash 문법·새 대화형 셸의 변수 존재를
값 노출 없이 확인했다. 개인 컨텍스트/압축 설정은 변경하지 않았다.

제공자 코드의 fast search 검증 요청 **1회 성공**, 생성형 요청0. 실제 키는 로그·Git·
스크린샷·공개 artifact에 저장하지 않았다. 이는 인증/응답 확인이며 전체 자료 수용의
완료를 뜻하지 않는다. 최종 앱의 암호화 연결 등록과 다중 도메인 탐색 검증을 이어 수행한다.

현재 [Exa Contents 공식 문서](https://exa.ai/docs/reference/get-contents)는 highlights를
LLM이 선택하는 발췌로 설명하므로 새 탐색에서는 highlights도 요청하지 않는다. 제한된
text 추출만 사용하고 summary/Answer/deep/outputSchema/systemPrompt/context를 제외한다.
검색엔진 자체의 내부 구현 전체를 비AI라고 주장하지 않는다. 공급 API가 제공한 발췌와
원문 직접 확인/재사용·보관 권리는 구분한다.

## 탐색 API와 실연결 중간 검증

네이버 HUB/기존 웹·블로그(최대2요청), Brave web(1요청), Exa fast+제한 text(1요청)를 지원한다. 연결은 명시 선택하며 같은 연결 quota를 실제 송신 전 예약한다. 제공자 오류·키 없음·비활성·부분 성공은 개별 결과다. 발췌는 HTML을 제거한 500자이며 API 응답 no-store, 원문/검색 이력 DB 저장0이다. 문서별 원문 도메인/URL/작성일/취득일/확인 수준과 발견 경로를 유지하고 추적 매개변수만 제거해 SKU/판본/fragment를 보존한다.

관심 조회는 기존 source adapter와 매칭을 사용하지만 LLM 재판정 경로를 호출하지 않는다. 조회 중 관심/소스 revision 변경 시 오래된 가격 저장을 막고 허용된 소스의 실제 취득만 관심 ID의 관측으로 남긴다. 실패 시 마지막 관측의 원래 시각을 유지한다. 외부 값 적용은 서버가 발급한 15분 증거와 현재 제품/소스/연결 revision을 확인해 선택한 필드만 바꾼다. 검색 제목의 명시 도수·숙성연수만 제안하며 본문의 작성연도/다른 술·주관적 맛/평점은 추론하지 않는다.

- API/순수 검색 집중 **33 passed**: 네이버·Brave·Exa 요청 형태/비생성 계약, 부분 실패, dedup, HTML/비밀 반사/비공개 URL 차단, 송신 전 취소와 guard 해제, 소유권/미등록·비활성, 관심 합계 제외/이력 허용, source·interest 동시 수정, 선택 적용/재시도 충돌/변조 및 연결 변경을 확인했다.
- Exa 실검색은 사용자 환경 키로 6주종×1요청=6회 성공. 문서60/발췌60, 원문 도메인 수는 주종별10/10/10/8/8/9. [본문 없는 실행 요약](assets/v170-b05-exa-coverage.json). 새 생성형 요청0, 원문/이미지 저장0. 정확한 판본 정답·데일리샷 미등록 여부·원문 직접 검증과 별개다.
- 새 UI/전체 회귀/실제 앱에 암호화 연결 등록은 진행 중이다. NAVER·Brave의 실제 키 인증, 국내2가격출처 권리/실연결은 미확정이며 합성 수용으로 대신하지 않는다.

B08 통합 `d788657` 기준 전체 Python **1205 passed/30 opt-in skipped**(아래 후속 제품 경로 이전). 실제 관심/소스 동시 수정은 새 관측0으로 확인했다. 후속 제품 탐색은 `/discovery/products/{id}/lookup`에서 기존 상품 고정/선호 판매처/캐시·관측을 재사용하고 `allow_llm=False`와 선택 source filter를 강제한다. 해당 API·기존 외부 조회 집중 **101 passed**; 관심 경로는 계속 별도 안정 ID다.

격리 PostgreSQL의 인증된 앱 API로 사용자 환경 Exa 키를 암호화 등록→명시 활성화→연결 확인→새 탐색까지 실검증했다. 앱의 minute/day 요청 한도를2로 제한했고 실제 예약 **2/2**, 연결/검색 성공, 문서10개, 응답의 키 원문0/no-store를 확인했다. [키/본문 없는 앱 실행 요약](assets/v170-b05-exa-app.json). 최초 harness의 잘못된 probe 경로/필수 revision 누락은 송신 전에 수정했고 성공 요청 수에 포함하지 않았다. 운영 계정 설정은 아직 변경하지 않았다.
